{% extends 'base.jinja' %}
{% block content %}
    <div class="items__wrapper">
        <h3>
            
                <a href="/items/add">Add Item</a>
            
        </h3>    

        <ul>
            {% for item in items %}
                <li><a href="/item/{{item.id}}">{{ item.item_name }}</a>, {{ item.item_price }}</li>
            {% endfor %}
        </ul>
    </div>

{% endblock content %}