{% extends 'base.jinja' %}{% block content %}
    <Div class="edit__wrapper">

        <div class="edit__header orders__header">
            Editing Order {{ order.id }}
        </div>

        <div class="edit__form">
            <form action="/orders/{{order.id}}" method="post">
                <label for="customer_name">Customer Name:</label>
                <input type="text" name="customer_name" id="customer_name" value="{{order.customer_name}}">

                <label for="customer_phone">Phone:</label>
                <input type="text" id="customer_phone" name="customer_phone" 
                    pattern="[0-9]{10}" title="Requires 10 numbers" value="{{phone}}"/>
            
                <label for="customer_address">Address</label>
                <input type="text" name="customer_address" id="customer_address" value="{{order.customer_address}}">

                <div class="delivered_box">
                    <label for="delivered">Revert Order Status</label>
                    {% if order.order_delivered == True %}
                    <input type="checkbox" name="delivered" id="delivered" checked>
                     {% else %}
                    <input type="checkbox" name="delivered" id="delivered" >
                    {% endif %}


                </div>
                
                <button class="save_button" type="submit" onclick="return confirm('Confirm Edit!')">Save Order</button>
            </form>
        </div>

    </Div>
{% endblock content %}